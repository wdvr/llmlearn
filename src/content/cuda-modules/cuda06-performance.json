{
  "id": "cuda-performance",
  "title": "Performance Optimization",
  "description": "Master GPU performance analysis through memory coalescing, the roofline model for identifying bottlenecks, and systematic profiling techniques to iteratively optimize your CUDA kernels.",
  "course": "cuda-parallel",
  "sections": [
    {
      "title": "Memory Coalescing Deep Dive",
      "content": "## The #1 Performance Factor for GPU Kernels\n\nIf you only learn one GPU optimization technique, let it be **memory coalescing**. The difference between coalesced and uncoalesced memory access can be **10-20x** in performance. No amount of occupancy tuning or divergence elimination will save a kernel with terrible memory access patterns.\n\n## What Is Memory Coalescing?\n\nGlobal memory (GPU DRAM) is accessed in **transactions** of 32 or 128 bytes. When threads in a warp access memory, the hardware attempts to **combine (coalesce)** their requests into the minimum number of transactions.\n\n**Coalesced access**: All 32 threads in a warp access consecutive memory addresses. The hardware combines this into a single 128-byte transaction. One trip to memory serves all 32 threads.\n\n**Uncoalesced access**: Threads access scattered or strided addresses. Each thread's request may require a separate transaction. Instead of 1 transaction, you might need 32 transactions -- a **32x** increase in memory traffic.\n\nThink of it like a library. Coalesced access is like requesting pages 1-32 of a book -- the librarian grabs one volume. Uncoalesced access is like requesting page 1 from book A, page 47 from book B, page 203 from book C -- the librarian makes 32 separate trips.\n\n## How Coalescing Works at the Hardware Level\n\nGlobal memory is divided into **128-byte segments** (also called cache lines). When a warp issues a memory request:\n\n1. The hardware collects all 32 thread addresses\n2. It determines which 128-byte segments are touched\n3. It issues one transaction per segment\n\n**Best case** (perfectly coalesced): All 32 threads access consecutive `float32` values (32 x 4 bytes = 128 bytes). One segment, one transaction.\n\n**Worst case** (fully strided): Each thread accesses a different segment. 32 segments, 32 transactions. Same data, 32x more memory bandwidth consumed.\n\n```\nPerfectly Coalesced (1 transaction):\nThread  0 -> address 0x000    |\nThread  1 -> address 0x004    |  All within the same\nThread  2 -> address 0x008    |  128-byte segment\n...                            |\nThread 31 -> address 0x07C    |\n\nFully Strided (up to 32 transactions):\nThread  0 -> address 0x000     <- Segment 0\nThread  1 -> address 0x200     <- Segment 4\nThread  2 -> address 0x400     <- Segment 8\n...                              (each in a different segment)\nThread 31 -> address 0x3E00    <- Segment 124\n```\n\n## The Classic Example: Row-Major vs Column-Major Access\n\nConsider a 2D matrix stored in **row-major** order (C/numpy default), where element `M[row][col]` is at memory address `base + row * num_cols + col`.\n\n```python\n# Row-major layout in memory:\n# M[0][0], M[0][1], M[0][2], ..., M[0][N-1],  <- Row 0\n# M[1][0], M[1][1], M[1][2], ..., M[1][N-1],  <- Row 1\n# ...\n```\n\n**Coalesced pattern** -- threads read along a row:\n```python\n@cuda.jit\ndef read_row(matrix, output, rows, cols):\n    row = cuda.blockIdx.x\n    col = cuda.threadIdx.x  # Consecutive threads -> consecutive columns\n    if row < rows and col < cols:\n        output[row * cols + col] = matrix[row, col]\n    # Thread 0 reads M[row][0], Thread 1 reads M[row][1], ...\n    # These are consecutive in memory -> COALESCED!\n```\n\n**Uncoalesced pattern** -- threads read along a column:\n```python\n@cuda.jit\ndef read_col(matrix, output, rows, cols):\n    col = cuda.blockIdx.x\n    row = cuda.threadIdx.x  # Consecutive threads -> consecutive rows\n    if row < rows and col < cols:\n        output[row * cols + col] = matrix[row, col]\n    # Thread 0 reads M[0][col], Thread 1 reads M[1][col], ...\n    # These are COLS apart in memory -> UNCOALESCED!\n    # Stride = cols * 4 bytes. If cols=1024, stride=4096 bytes.\n    # Each thread hits a different 128-byte segment.\n```\n\nThe performance difference is dramatic:\n\n| Access Pattern | Transactions per warp | Relative Performance |\n|---------------|----------------------|---------------------|\n| Row-wise (coalesced) | 1 | 1.0x (baseline) |\n| Column-wise (stride = N) | up to 32 | 0.05x - 0.1x |\n| Random | up to 32 | 0.03x - 0.1x |\n\n## Quantifying the Penalty\n\nLet us calculate the bandwidth waste. On a T4 GPU with ~320 GB/s memory bandwidth:\n\n**Coalesced**: Reading 1M float32 values = 4 MB. At 320 GB/s, this takes ~13 microseconds.\n\n**Strided (stride = 1024)**: Each warp reads 32 values (128 bytes of useful data) but triggers 32 transactions of 128 bytes each (4096 bytes transferred). The ratio of useful to transferred data is 128/4096 = **3.1%**. The effective bandwidth drops to ~9.4 GB/s. The same 4 MB read now takes ~425 microseconds -- **32x slower**.\n\nThis is not a theoretical exercise. Uncoalesced access is the most common performance bug in GPU code.\n\n## Restructuring for Coalescing\n\n### Technique 1: Transpose Your Thinking\n\nIf your algorithm naturally processes columns, transpose the matrix first:\n\n```python\n# Instead of processing columns of a row-major matrix (bad):\n# Process rows of the transposed matrix (good)!\nmatrix_T = matrix.T.copy()  # Transpose on CPU\nd_matrix_T = cuda.to_device(matrix_T)\n# Now row access on the transpose = column access on the original\n# But the memory access is coalesced!\n```\n\nThe transpose costs O(N^2) but is a one-time cost. If you process the matrix many times, the coalesced access saves far more than the transpose costs.\n\n### Technique 2: Structure of Arrays (SoA) vs Array of Structures (AoS)\n\nThis is one of the most important data layout decisions in GPU programming:\n\n```python\n# Array of Structures (AoS) -- BAD for GPU coalescing\n# particles = [(x0,y0,z0,m0), (x1,y1,z1,m1), (x2,y2,z2,m2), ...]\n# Memory: x0,y0,z0,m0, x1,y1,z1,m1, x2,y2,z2,m2, ...\n# If threads read all x-values: stride of 4 floats between each x -> uncoalesced\n\n# Structure of Arrays (SoA) -- GOOD for GPU coalescing\n# x = [x0, x1, x2, ...]\n# y = [y0, y1, y2, ...]\n# z = [z0, z1, z2, ...]\n# m = [m0, m1, m2, ...]\n# If threads read all x-values: consecutive in memory -> coalesced!\n```\n\nOn CPUs, AoS is often preferred because it keeps related data in the same cache line (good for processing one particle at a time). On GPUs, SoA is almost always better because adjacent threads process adjacent elements, and you want those elements consecutive in memory.\n\n### Technique 3: Shared Memory as a Coalescing Buffer\n\nWhen your algorithm requires non-coalesced access patterns, use shared memory as an intermediary:\n\n1. Load data from global memory with **coalesced** access into shared memory\n2. Rearrange the data in shared memory (fast, no coalescing needed)\n3. Process from shared memory in whatever order your algorithm requires\n\n```python\n@cuda.jit\ndef transpose_tiled(input_matrix, output_matrix, rows, cols):\n    TILE = 32\n    tile = cuda.shared.array((TILE, TILE), dtype=float32)\n    \n    # Coalesced read from input\n    x = cuda.blockIdx.x * TILE + cuda.threadIdx.x\n    y = cuda.blockIdx.y * TILE + cuda.threadIdx.y\n    if x < cols and y < rows:\n        tile[cuda.threadIdx.y, cuda.threadIdx.x] = input_matrix[y, x]\n    \n    cuda.syncthreads()\n    \n    # Coalesced write to output (transposed indices)\n    x = cuda.blockIdx.y * TILE + cuda.threadIdx.x\n    y = cuda.blockIdx.x * TILE + cuda.threadIdx.y\n    if x < rows and y < cols:\n        output_matrix[y, x] = tile[cuda.threadIdx.x, cuda.threadIdx.y]\n```\n\nBoth the read and write are coalesced (consecutive threads access consecutive elements), even though we are performing a transpose.\n\n## Common Coalescing Mistakes\n\n| Mistake | Why It Happens | Fix |\n|---------|---------------|-----|\n| Column access on row-major matrix | Natural for some algorithms | Transpose or use shared memory tiling |\n| Accessing every Nth element | Strided algorithms | Restructure to consecutive access |\n| AoS data layout | Habit from CPU programming | Convert to SoA |\n| Random access (hash tables, graphs) | Algorithmic requirement | Use shared memory, texture cache, or sort data |\n\n## Key Takeaways\n\n1. **Coalesced access**: consecutive threads access consecutive addresses -- the hardware combines into 1-4 transactions\n2. **Uncoalesced access**: strided or random patterns can require up to 32x more transactions\n3. **Row access on row-major data is coalesced**; column access is not\n4. **Structure of Arrays (SoA)** layout is almost always better than AoS for GPUs\n5. **Shared memory** can serve as a coalescing buffer for algorithms requiring non-sequential access",
      "code": "!pip install numba\n\nfrom numba import cuda\nimport numpy as np\nimport math\nimport time\n\n# ------------------------------------------------------------------\n# Experiment: Coalesced vs Strided Memory Access\n# ------------------------------------------------------------------\n\n# Kernel: coalesced row-wise read of a 2D matrix\n@cuda.jit\ndef read_coalesced(matrix, output, rows, cols):\n    \"\"\"Each thread reads one element; consecutive threads read consecutive columns.\"\"\"\n    row = cuda.blockIdx.y * cuda.blockDim.y + cuda.threadIdx.y\n    col = cuda.blockIdx.x * cuda.blockDim.x + cuda.threadIdx.x\n    if row < rows and col < cols:\n        # Consecutive threads (varying threadIdx.x) read consecutive cols\n        # -> consecutive memory addresses -> COALESCED\n        output[row * cols + col] = matrix[row, col] * 2.0\n\n# Kernel: uncoalesced column-wise read of a 2D matrix\n@cuda.jit\ndef read_strided(matrix, output, rows, cols):\n    \"\"\"Each thread reads one element; consecutive threads read consecutive rows.\"\"\"\n    col = cuda.blockIdx.y * cuda.blockDim.y + cuda.threadIdx.y\n    row = cuda.blockIdx.x * cuda.blockDim.x + cuda.threadIdx.x\n    if row < rows and col < cols:\n        # Consecutive threads (varying threadIdx.x) read consecutive rows\n        # -> addresses are 'cols' apart -> STRIDED -> UNCOALESCED\n        output[row * cols + col] = matrix[row, col] * 2.0\n\ndef bench_kernel(kernel, args, grid, block, warmup=10, iters=50):\n    for _ in range(warmup):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    start = time.perf_counter()\n    for _ in range(iters):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    return (time.perf_counter() - start) / iters * 1000  # ms\n\n# Setup: large matrix\nrows, cols = 4096, 4096\nmatrix = np.random.randn(rows, cols).astype(np.float32)\nd_matrix = cuda.to_device(matrix)\nd_output = cuda.device_array(rows * cols, dtype=np.float32)\n\n# Block and grid for coalesced (x = col, y = row)\nBLOCK = (32, 8)\ngrid_coal = (math.ceil(cols / BLOCK[0]), math.ceil(rows / BLOCK[1]))\n\n# Block and grid for strided (x = row, y = col)\ngrid_stri = (math.ceil(rows / BLOCK[0]), math.ceil(cols / BLOCK[1]))\n\ndata_bytes = rows * cols * 4  # float32\n\nt_coal = bench_kernel(read_coalesced, (d_matrix, d_output, rows, cols), grid_coal, BLOCK)\nt_stri = bench_kernel(read_strided, (d_matrix, d_output, rows, cols), grid_stri, BLOCK)\n\nbw_coal = (data_bytes * 2) / (t_coal / 1000) / 1e9  # GB/s (read + write)\nbw_stri = (data_bytes * 2) / (t_stri / 1000) / 1e9\n\nprint(\"=== Memory Coalescing Experiment ===\")\nprint(f\"Matrix: {rows} x {cols} float32 ({data_bytes / 1e6:.1f} MB)\\n\")\nprint(f\"{'Pattern':<25} {'Time (ms)':<14} {'Bandwidth (GB/s)':<20} {'Relative'}\")\nprint(\"-\" * 70)\nprint(f\"{'Coalesced (row-wise)':<25} {t_coal:<14.3f} {bw_coal:<20.1f} {'1.00x (baseline)'}\")\nprint(f\"{'Strided (col-wise)':<25} {t_stri:<14.3f} {bw_stri:<20.1f} {t_stri/t_coal:.1f}x slower\")\nprint(f\"\\nBandwidth utilization:\")\nprint(f\"  Coalesced: {bw_coal:.1f} GB/s\")\nprint(f\"  Strided:   {bw_stri:.1f} GB/s\")\nprint(f\"  Efficiency ratio: {bw_stri/bw_coal*100:.1f}%\")\n\n# ------------------------------------------------------------------\n# Experiment 2: Stride size impact\n# ------------------------------------------------------------------\n@cuda.jit\ndef read_with_stride(data, output, n, stride):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        src_idx = (idx * stride) % n  # Wrap around\n        output[idx] = data[src_idx] * 2.0\n\nn = 2_000_000\nd_data = cuda.to_device(np.random.randn(n).astype(np.float32))\nd_out = cuda.device_array(n, dtype=np.float32)\nTPB = 256\nBPG = math.ceil(n / TPB)\n\nprint(\"\\n=== Stride Size vs Performance ===\")\nprint(f\"{'Stride':<10} {'Time (ms)':<14} {'Effective BW (GB/s)':<22} {'Slowdown'}\")\nprint(\"-\" * 60)\n\nbaseline = None\nfor stride in [1, 2, 4, 8, 16, 32, 64, 128]:\n    t = bench_kernel(read_with_stride, (d_data, d_out, n, stride), BPG, TPB)\n    bw = (n * 4 * 2) / (t / 1000) / 1e9\n    if baseline is None:\n        baseline = t\n    label = \"(coalesced)\" if stride == 1 else \"\"\n    print(f\"{stride:<10} {t:<14.4f} {bw:<22.1f} {t/baseline:.2f}x {label}\")",
      "references": [
        {
          "title": "NVIDIA CUDA Best Practices: Coalesced Access to Global Memory",
          "url": "https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html#coalesced-access-to-global-memory"
        },
        {
          "title": "PMPP Chapter 5: Memory Coalescing",
          "url": "https://www.elsevier.com/books/programming-massively-parallel-processors/hwu/978-0-323-91231-0"
        },
        {
          "title": "Structure of Arrays vs Array of Structures (NVIDIA Developer Blog)",
          "url": "https://developer.nvidia.com/blog/how-access-global-memory-efficiently-cuda-c-kernels/"
        }
      ]
    },
    {
      "title": "Arithmetic Intensity & Roofline Model",
      "content": "## Why Some Kernels Are Fast and Others Are Not\n\nYou have learned about memory coalescing (maximizing bandwidth utilization) and warp divergence (maximizing compute utilization). But how do you know which one matters more for **your specific kernel**? The answer lies in a concept called **arithmetic intensity** and a visual tool called the **roofline model**.\n\n## Arithmetic Intensity: FLOPS per Byte\n\nArithmetic intensity (AI) is the ratio of floating-point operations (FLOPS) to bytes transferred from memory:\n\n```\nArithmetic Intensity = FLOPS / Bytes Transferred\n```\n\nA kernel that performs 100 floating-point operations but loads 400 bytes of data has an AI of 100/400 = **0.25 FLOPS/byte**. This is a **memory-bound** kernel -- its performance is limited by how fast it can feed data to the compute units.\n\nA kernel that performs 10,000 floating-point operations on 400 bytes of data has an AI of 10,000/400 = **25 FLOPS/byte**. This is a **compute-bound** kernel -- its performance is limited by the raw compute throughput of the GPU.\n\n### Example: Vector Addition\n\n```python\n@cuda.jit\ndef vector_add(a, b, c, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        c[idx] = a[idx] + b[idx]  # 1 FLOP, 12 bytes (2 reads + 1 write, float32)\n```\n\n- FLOPS per element: 1 (the addition)\n- Bytes per element: 12 (read `a[idx]` = 4B, read `b[idx]` = 4B, write `c[idx]` = 4B)\n- AI = 1/12 = **0.083 FLOPS/byte** -- extremely memory-bound!\n\n### Example: Matrix Multiplication\n\n```python\n# For N x N matrices:\n# Each output element requires N multiply-adds = 2N FLOPS\n# Total FLOPS: N^2 * 2N = 2N^3\n# Total data: 3 matrices * N^2 * 4 bytes = 12N^2 bytes\n# AI = 2N^3 / 12N^2 = N/6 FLOPS/byte\n```\n\nFor N=1024: AI = 1024/6 = **170 FLOPS/byte** -- compute-bound!\n\nThis is why matrix multiplication scales so well on GPUs and vector addition does not. Matrix multiply reuses each loaded element N times, amortizing the memory cost.\n\n| Kernel | FLOPS/element | Bytes/element | AI (FLOPS/byte) | Bound |\n|--------|--------------|---------------|-----------------|-------|\n| Vector add | 1 | 12 | 0.08 | Memory |\n| Vector multiply-add | 2 | 16 | 0.13 | Memory |\n| Dot product (per elem) | 2 | 8 | 0.25 | Memory |\n| Stencil (5-point) | 5 | 4 (with reuse) | 1.25 | Memory |\n| Matrix multiply (N=1024) | 2048 | 12 | 170 | Compute |\n| Convolution (7x7 filter) | 98 | 4 | 24.5 | Balanced |\n\n## The Roofline Model\n\nThe roofline model is a visual tool that shows the **maximum achievable performance** of a kernel given its arithmetic intensity and the hardware capabilities.\n\nThe model has two components:\n\n1. **Memory bandwidth ceiling**: A sloped line representing `Performance = AI x Bandwidth`. For memory-bound kernels, performance grows linearly with AI.\n\n2. **Compute throughput ceiling**: A flat line representing the peak FLOPS of the GPU.\n\nThe **roofline** is the minimum of these two:\n\n```\nAttainable Performance = min(Peak_FLOPS, AI x Peak_Bandwidth)\n```\n\nThe **ridge point** is where the two lines meet:\n```\nRidge Point AI = Peak_FLOPS / Peak_Bandwidth\n```\n\nFor a T4 GPU:\n- Peak FP32 compute: ~8.1 TFLOPS\n- Peak memory bandwidth: ~320 GB/s\n- Ridge point: 8100 / 320 = **25.3 FLOPS/byte**\n\nKernels with AI < 25.3 are memory-bound. Kernels with AI > 25.3 are compute-bound.\n\n```\nPerformance (GFLOPS)\n        |\n  8100  |                    __________________ (Compute ceiling)\n        |                   /\n        |                  /\n        |                 /\n        |                / <- Memory BW ceiling (slope = 320 GB/s)\n        |               /\n        |              /\n        |             /\n        |            /\n        |           /\n        |__________/________________________________\n                 25.3                  AI (FLOPS/byte)\n              Ridge Point\n```\n\n## Where Does Your Kernel Sit?\n\nTo use the roofline model:\n\n1. **Calculate your kernel's AI** -- count FLOPS and bytes per element\n2. **Plot it on the x-axis** of the roofline chart\n3. **Read up to the roofline** -- that is your theoretical maximum performance\n4. **Compare to actual performance** -- the gap shows optimization potential\n\nIf your kernel is **below the memory bandwidth roof**:\n- You are memory-bound\n- Optimize memory access patterns (coalescing, caching)\n- Consider algorithmic changes to increase reuse\n- Improving compute (reducing divergence) will NOT help\n\nIf your kernel is **below the compute roof**:\n- You are compute-bound\n- Optimize instruction mix (use faster operations, FMA)\n- Increase occupancy for better latency hiding\n- Memory optimizations will NOT help\n\n## Practical AI Calculation\n\nHere is how to calculate AI for a real kernel:\n\n```python\n@cuda.jit\ndef saxpy(a, x, y, output, n):\n    # y = a * x + y  (SAXPY: Single-precision A*X Plus Y)\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        output[idx] = a * x[idx] + y[idx]\n    # FLOPS: 1 multiply + 1 add = 2 FLOPS per element\n    # Bytes: read x[idx] (4B) + read y[idx] (4B) + write output[idx] (4B) = 12 B\n    # AI = 2/12 = 0.167 FLOPS/byte\n    # On T4: max perf = 0.167 * 320 GB/s = 53 GFLOPS\n    # That's only 0.6% of peak compute (8100 GFLOPS)\n    # -> This kernel can NEVER be compute-bound. Focus on memory!\n```\n\n## Improving Arithmetic Intensity\n\nThe key strategies for improving AI (and thus moving from memory-bound to compute-bound):\n\n1. **Data reuse through tiling**: Load data into shared memory once, use it multiple times. Matrix multiply uses this extensively.\n\n2. **Kernel fusion**: Instead of running two kernels (read-compute-write, read-compute-write), fuse them into one (read-compute-compute-write). Eliminates the intermediate write and read.\n\n```python\n# BAD: Two separate kernels (low AI each)\n@cuda.jit\ndef step1(a, b, temp, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        temp[idx] = a[idx] * b[idx]  # Write to global memory\n\n@cuda.jit\ndef step2(temp, c, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        output[idx] = temp[idx] + c[idx]  # Read from global memory\n\n# GOOD: Fused kernel (higher AI)\n@cuda.jit\ndef fused(a, b, c, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        output[idx] = a[idx] * b[idx] + c[idx]  # No intermediate storage\n```\n\n3. **Loop within kernel**: Process multiple elements per thread to increase the compute-to-load ratio.\n\n## The Gap Between Roofline and Reality\n\nReal kernels rarely hit the roofline. Common reasons for the gap:\n\n- **Uncoalesced memory access**: Wastes bandwidth, reducing effective memory throughput\n- **Warp divergence**: Wastes compute cycles, reducing effective FLOPS\n- **Low occupancy**: Cannot fully hide latency, leaving execution units idle\n- **Instruction overhead**: Index calculations, boundary checks, etc. consume cycles without useful FLOPS\n- **Bank conflicts**: Shared memory bank conflicts serialize parallel accesses\n\nThe optimization process is iterative: measure, identify the bottleneck, fix it, repeat.\n\n## Key Takeaways\n\n1. **Arithmetic intensity (AI) = FLOPS / Bytes** -- determines whether your kernel is memory-bound or compute-bound\n2. **Most GPU kernels are memory-bound** -- vector operations, reductions, and stencils all have low AI\n3. **The roofline model** visually shows max achievable performance given AI and hardware specs\n4. **Memory-bound kernels**: optimize coalescing, caching, and data layout\n5. **Compute-bound kernels**: optimize instruction mix, occupancy, and divergence\n6. **Kernel fusion and tiling** are the primary ways to increase AI",
      "code": "!pip install numba\n\nfrom numba import cuda\nimport numpy as np\nimport math\nimport time\n\n# ------------------------------------------------------------------\n# Demo 1: Calculate arithmetic intensity for common kernels\n# ------------------------------------------------------------------\nprint(\"=== Arithmetic Intensity of Common Kernels ===\")\nprint(f\"{'Kernel':<25} {'FLOPS/elem':<12} {'Bytes/elem':<12} {'AI (F/B)':<10} {'Bound'}\")\nprint(\"-\" * 72)\n\nkernels = [\n    (\"Vector add (a+b)\",       1,  12, \"Memory\"),\n    (\"SAXPY (a*x+y)\",          2,  12, \"Memory\"),\n    (\"Vector norm (x*x)\",      1,   8, \"Memory\"),\n    (\"5-pt stencil\",           5,  24, \"Memory\"),\n    (\"7x7 Convolution\",       98,   4, \"Balanced\"),\n    (\"MatMul 256x256\",       512,  12, \"Compute\"),\n    (\"MatMul 1024x1024\",    2048,  12, \"Compute\"),\n]\nfor name, flops, bytes_, bound in kernels:\n    ai = flops / bytes_\n    print(f\"{name:<25} {flops:<12} {bytes_:<12} {ai:<10.3f} {bound}\")\n\n# T4 ridge point\nt4_peak_gflops = 8100  # FP32\nt4_peak_bw = 320       # GB/s\nridge = t4_peak_gflops / t4_peak_bw\nprint(f\"\\nT4 Ridge Point: {ridge:.1f} FLOPS/byte\")\nprint(f\"Kernels with AI < {ridge:.1f} are memory-bound\")\nprint(f\"Kernels with AI > {ridge:.1f} are compute-bound\")\n\n# ------------------------------------------------------------------\n# Demo 2: Measure actual performance vs roofline\n# ------------------------------------------------------------------\n@cuda.jit\ndef vector_add(a, b, c, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        c[idx] = a[idx] + b[idx]\n\n@cuda.jit\ndef saxpy(scalar, x, y, out, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        out[idx] = scalar * x[idx] + y[idx]\n\n@cuda.jit\ndef heavy_compute(a, b, out, n):\n    \"\"\"Many FLOPs per element (high AI).\"\"\"\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        val = a[idx]\n        for _ in range(100):  # 100 iterations of 2 FLOPs = 200 FLOPs\n            val = val * b[idx] + a[idx]  # FMA (1 multiply + 1 add)\n        out[idx] = val\n\ndef bench(kernel, args, grid, block, warmup=10, iters=50):\n    for _ in range(warmup):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    start = time.perf_counter()\n    for _ in range(iters):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    return (time.perf_counter() - start) / iters * 1000\n\nn = 4_000_000\nTPB = 256\nBPG = math.ceil(n / TPB)\n\na = cuda.to_device(np.random.randn(n).astype(np.float32))\nb = cuda.to_device(np.random.randn(n).astype(np.float32))\nc = cuda.device_array(n, dtype=np.float32)\n\nprint(\"\\n=== Measured Performance vs Roofline ===\")\nprint(f\"{'Kernel':<20} {'Time (ms)':<12} {'AI (F/B)':<10} {'GFLOPS':<10} \"\n      f\"{'Eff BW (GB/s)':<15} {'Bottleneck'}\")\nprint(\"-\" * 80)\n\n# Vector add: 1 FLOP, 12 bytes\nt = bench(vector_add, (a, b, c, n), BPG, TPB)\nflops = n * 1\nbytes_moved = n * 12\ngflops = flops / (t / 1000) / 1e9\neff_bw = bytes_moved / (t / 1000) / 1e9\nai = 1 / 12\nprint(f\"{'Vector add':<20} {t:<12.4f} {ai:<10.3f} {gflops:<10.2f} {eff_bw:<15.1f} {'Memory'}\")\n\n# SAXPY: 2 FLOPS, 12 bytes\nt = bench(saxpy, (2.0, a, b, c, n), BPG, TPB)\nflops = n * 2\nbytes_moved = n * 12\ngflops = flops / (t / 1000) / 1e9\neff_bw = bytes_moved / (t / 1000) / 1e9\nai = 2 / 12\nprint(f\"{'SAXPY':<20} {t:<12.4f} {ai:<10.3f} {gflops:<10.2f} {eff_bw:<15.1f} {'Memory'}\")\n\n# Heavy compute: ~200 FLOPS, 12 bytes\nt = bench(heavy_compute, (a, b, c, n), BPG, TPB)\nflops = n * 200\nbytes_moved = n * 12\ngflops = flops / (t / 1000) / 1e9\neff_bw = bytes_moved / (t / 1000) / 1e9\nai = 200 / 12\nprint(f\"{'Heavy compute':<20} {t:<12.4f} {ai:<10.3f} {gflops:<10.2f} {eff_bw:<15.1f} {'Compute'}\")\n\n# ------------------------------------------------------------------\n# Demo 3: Kernel fusion benefit\n# ------------------------------------------------------------------\n@cuda.jit\ndef step1(a, b, temp, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        temp[idx] = a[idx] * b[idx]\n\n@cuda.jit\ndef step2(temp, b, out, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        out[idx] = temp[idx] + b[idx]\n\n@cuda.jit\ndef fused(a, b, out, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        out[idx] = a[idx] * b[idx] + b[idx]\n\ntemp = cuda.device_array(n, dtype=np.float32)\n\n# Two-kernel approach\ndef bench_two_kernels():\n    step1[BPG, TPB](a, b, temp, n)\n    step2[BPG, TPB](temp, b, c, n)\n\nfor _ in range(10):\n    bench_two_kernels()\ncuda.synchronize()\nstart = time.perf_counter()\nfor _ in range(50):\n    bench_two_kernels()\ncuda.synchronize()\nt_two = (time.perf_counter() - start) / 50 * 1000\n\nt_fused = bench(fused, (a, b, c, n), BPG, TPB)\n\nprint(f\"\\n=== Kernel Fusion Benefit ===\")\nprint(f\"Two separate kernels: {t_two:.4f} ms\")\nprint(f\"Fused kernel:         {t_fused:.4f} ms\")\nprint(f\"Speedup from fusion:  {t_two / t_fused:.2f}x\")\nprint(f\"(Fusion eliminates intermediate global memory write + read)\")",
      "references": [
        {
          "title": "Roofline Model (Berkeley, Original Paper)",
          "url": "https://people.eecs.berkeley.edu/~kubitron/cs252/handouts/papers/RooflineVywormageLBNL.pdf"
        },
        {
          "title": "PMPP Chapter 5: Performance Considerations",
          "url": "https://www.elsevier.com/books/programming-massively-parallel-processors/hwu/978-0-323-91231-0"
        },
        {
          "title": "NVIDIA T4 GPU Specifications",
          "url": "https://www.nvidia.com/en-us/data-center/tesla-t4/"
        }
      ]
    },
    {
      "title": "Profiling & Optimization Loop",
      "content": "## The Scientific Method for GPU Optimization\n\nOptimizing GPU code without measurement is like navigating without a compass -- you might get lucky, but you will probably go in circles. The profiling-driven optimization loop is:\n\n1. **Measure** current performance (time, bandwidth, FLOPS)\n2. **Identify** the bottleneck (memory-bound? compute-bound? latency-bound?)\n3. **Hypothesize** a fix (coalescing? occupancy? fusion?)\n4. **Apply** the fix\n5. **Measure** again to verify improvement\n6. **Repeat** until satisfied\n\nThis is the scientific method applied to performance engineering. Never skip steps 1 and 5 -- they are the most important.\n\n## CUDA Events for Precise Timing\n\nPython's `time.perf_counter()` measures wall-clock time, which includes Python overhead and is subject to OS scheduling noise. CUDA events provide **GPU-side timing** that is far more precise.\n\nIn numba.cuda, we use `cuda.event()` for creating events and measure elapsed time between them:\n\n```python\nfrom numba import cuda\nimport time\n\n# Method 1: Python-side timing with synchronization\ndef time_kernel_python(kernel, args, grid, block, iters=100):\n    # Warmup\n    for _ in range(10):\n        kernel[grid, block](*args)\n    cuda.synchronize()  # CRITICAL: ensure GPU is done before timing\n    \n    start = time.perf_counter()\n    for _ in range(iters):\n        kernel[grid, block](*args)\n    cuda.synchronize()  # CRITICAL: ensure GPU is done after timing\n    end = time.perf_counter()\n    \n    return (end - start) / iters * 1000  # ms\n```\n\n### Why Warmup Matters\n\nThe first kernel launch is always slow due to:\n- **JIT compilation**: Numba compiles CUDA kernels on first invocation\n- **Context initialization**: CUDA context setup on first GPU operation\n- **Memory allocation**: First `device_array` or `to_device` triggers GPU memory setup\n- **Cache cold-start**: L2 cache is empty on first access\n\nAlways run 5-10 warmup iterations before timing. Compare:\n\n```python\n# Without warmup: first run includes JIT compilation (~100ms+)\n# With warmup: subsequent runs reflect true kernel time (~0.1ms)\n```\n\nThe difference can be **1000x**, completely invalidating your measurements.\n\n## Building a Proper Benchmark Harness\n\nA robust benchmark function should:\n\n1. Accept any kernel with its arguments\n2. Perform warmup iterations\n3. Synchronize before and after the timed region\n4. Run enough iterations to average out noise\n5. Report multiple statistics (mean, min, max, std)\n\n```python\ndef benchmark_kernel(kernel, grid, block, args, warmup=10, iters=100):\n    \"\"\"Robust GPU kernel benchmarking.\"\"\"\n    # Warmup (includes JIT compilation on first call)\n    for _ in range(warmup):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    \n    # Timed iterations\n    times = []\n    for _ in range(iters):\n        cuda.synchronize()\n        start = time.perf_counter()\n        kernel[grid, block](*args)\n        cuda.synchronize()\n        end = time.perf_counter()\n        times.append((end - start) * 1000)  # ms\n    \n    times = np.array(times)\n    return {\n        'mean': np.mean(times),\n        'std': np.std(times),\n        'min': np.min(times),\n        'max': np.max(times),\n        'median': np.median(times)\n    }\n```\n\n## Identifying Bottlenecks\n\nOnce you have timing data, the next step is determining **why** your kernel is slow. Here is a systematic approach:\n\n### Step 1: Calculate Achieved Bandwidth and FLOPS\n\n```python\ndef analyze_kernel(time_ms, data_bytes, flops):\n    \"\"\"Determine if kernel is memory-bound or compute-bound.\"\"\"\n    achieved_bw = data_bytes / (time_ms / 1000) / 1e9  # GB/s\n    achieved_gflops = flops / (time_ms / 1000) / 1e9    # GFLOPS\n    ai = flops / data_bytes                               # FLOPS/byte\n    \n    # T4 specs\n    peak_bw = 320      # GB/s\n    peak_gflops = 8100  # GFLOPS FP32\n    \n    bw_utilization = achieved_bw / peak_bw * 100\n    compute_utilization = achieved_gflops / peak_gflops * 100\n    \n    print(f\"Achieved bandwidth: {achieved_bw:.1f} GB/s ({bw_utilization:.1f}% of peak)\")\n    print(f\"Achieved compute: {achieved_gflops:.1f} GFLOPS ({compute_utilization:.1f}% of peak)\")\n    print(f\"Arithmetic intensity: {ai:.2f} FLOPS/byte\")\n    \n    if bw_utilization > compute_utilization:\n        print(\"Diagnosis: MEMORY-BOUND (bandwidth is the bottleneck)\")\n    else:\n        print(\"Diagnosis: COMPUTE-BOUND (compute is the bottleneck)\")\n```\n\n### Step 2: Check Memory Access Efficiency\n\nIf memory-bound, check whether you are achieving reasonable bandwidth:\n\n| Achieved BW | Diagnosis | Action |\n|------------|-----------|--------|\n| >80% of peak | Excellent; little room for improvement | Focus on algorithm changes |\n| 50-80% of peak | Good; some optimization possible | Check coalescing, consider tiling |\n| 20-50% of peak | Poor; likely uncoalesced access | Fix memory access patterns |\n| <20% of peak | Very poor; severe access pattern issues | Restructure data layout entirely |\n\n### Step 3: Check Compute Efficiency\n\nIf compute-bound, check whether you are achieving reasonable FLOPS:\n\n| Achieved FLOPS | Diagnosis | Action |\n|---------------|-----------|--------|\n| >50% of peak | Excellent | Minor tuning only |\n| 25-50% of peak | Good | Check occupancy, divergence |\n| <25% of peak | Poor | Major optimization needed |\n\n## The GPU Optimization Checklist\n\nWhen your kernel is too slow, work through this checklist **in order**. Each item addresses the most common performance issues, from most impactful to least:\n\n### Priority 1: Memory Access Patterns\n- [ ] Are global memory accesses coalesced? (Consecutive threads access consecutive addresses)\n- [ ] Is the data layout SoA rather than AoS?\n- [ ] Can you use shared memory to stage data for non-coalesced algorithms?\n- [ ] Are you reading/writing only the data you need? (No unnecessary loads)\n\n### Priority 2: Parallelism\n- [ ] Are you launching enough thread blocks to saturate all SMs?\n- [ ] Is the block size a multiple of 32 (ideally 128-256)?\n- [ ] Is occupancy at least 50%? (Check register and shared memory usage)\n- [ ] Can you increase the grid size by having each thread process one element?\n\n### Priority 3: Instruction Efficiency\n- [ ] Is warp divergence minimized? (Branch-free where possible)\n- [ ] Can expensive operations be replaced? (division -> multiplication, pow -> repeated multiply)\n- [ ] Can you use FMA (fused multiply-add) instructions?\n- [ ] Are loop bounds known at compile time? (Enables loop unrolling)\n\n### Priority 4: Kernel Launch Overhead\n- [ ] Can multiple kernels be fused into one?\n- [ ] Is the problem large enough to amortize launch overhead? (~5-10 microseconds per launch)\n- [ ] Are you doing unnecessary CPU-GPU synchronization between kernels?\n\n### Priority 5: Data Transfer\n- [ ] Are you minimizing CPU-GPU data transfers?\n- [ ] Can you keep data on the GPU between kernel launches?\n- [ ] Are you using pinned (page-locked) memory for necessary transfers?\n\n## A Complete Optimization Example\n\nLet us walk through optimizing a real kernel step by step.\n\n**Task**: Compute element-wise `output[i] = sqrt(a[i]^2 + b[i]^2)` for large arrays.\n\n**Version 1 (Naive)**:\n```python\n@cuda.jit\ndef magnitude_v1(a, b, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        output[idx] = math.sqrt(a[idx] ** 2 + b[idx] ** 2)\n```\n\n**Version 2 (Avoid pow, use multiply)**:\n```python\n@cuda.jit\ndef magnitude_v2(a, b, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        ai = a[idx]\n        bi = b[idx]\n        output[idx] = math.sqrt(ai * ai + bi * bi)  # multiply faster than **2\n```\n\n**Version 3 (Multiple elements per thread for better instruction-level parallelism)**:\n```python\n@cuda.jit\ndef magnitude_v3(a, b, output, n):\n    idx = (cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x) * 4\n    for i in range(4):  # Each thread processes 4 elements\n        if idx + i < n:\n            ai = a[idx + i]\n            bi = b[idx + i]\n            output[idx + i] = math.sqrt(ai * ai + bi * bi)\n```\n\nEach version addresses a different aspect: V1 is baseline, V2 reduces instruction cost, V3 increases instruction-level parallelism. Measuring each version tells you which optimization had the biggest impact.\n\n## Anti-Patterns: What NOT to Do\n\n1. **Don't optimize without measuring** -- you will waste time on non-bottlenecks\n2. **Don't chase 100% occupancy** -- 50-75% is usually sufficient\n3. **Don't add complexity for marginal gains** -- a 2% improvement is not worth 100 lines of code\n4. **Don't compare GPU time to unoptimized CPU code** -- always compare against numpy/optimized CPU\n5. **Don't forget warmup** -- JIT compilation will dominate your first measurement\n\n## Key Takeaways\n\n1. **Always measure before and after** every optimization\n2. **Use proper warmup and synchronization** for accurate timing\n3. **Calculate achieved bandwidth and FLOPS** to identify the bottleneck type\n4. **Follow the optimization checklist** in priority order\n5. **Stop when satisfied** -- diminishing returns are real",
      "code": "!pip install numba\n\nfrom numba import cuda\nimport numpy as np\nimport math\nimport time\n\n# ==================================================================\n# Complete profiling & optimization example\n# Task: compute output[i] = sqrt(a[i]^2 + b[i]^2)\n# ==================================================================\n\n# --- Benchmark Harness ---\ndef benchmark(kernel, grid, block, args, warmup=10, iters=50):\n    for _ in range(warmup):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    times = []\n    for _ in range(iters):\n        cuda.synchronize()\n        start = time.perf_counter()\n        kernel[grid, block](*args)\n        cuda.synchronize()\n        t = (time.perf_counter() - start) * 1000\n        times.append(t)\n    times = np.array(times)\n    return {'mean': np.mean(times), 'std': np.std(times),\n            'min': np.min(times), 'median': np.median(times)}\n\ndef analyze(label, time_ms, n, flops_per_elem, bytes_per_elem):\n    total_flops = n * flops_per_elem\n    total_bytes = n * bytes_per_elem\n    gflops = total_flops / (time_ms / 1000) / 1e9\n    bw = total_bytes / (time_ms / 1000) / 1e9\n    ai = flops_per_elem / bytes_per_elem\n    print(f\"{label:<24} {time_ms:>8.4f} ms | {bw:>7.1f} GB/s | {gflops:>7.1f} GFLOPS | AI={ai:.2f}\")\n    return time_ms\n\n# --- Kernel versions ---\n@cuda.jit\ndef magnitude_v1(a, b, output, n):\n    \"\"\"Naive: uses ** operator.\"\"\"\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        output[idx] = math.sqrt(a[idx] ** 2 + b[idx] ** 2)\n\n@cuda.jit\ndef magnitude_v2(a, b, output, n):\n    \"\"\"Better: uses multiply instead of pow.\"\"\"\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        ai = a[idx]\n        bi = b[idx]\n        output[idx] = math.sqrt(ai * ai + bi * bi)\n\n@cuda.jit\ndef magnitude_v3(a, b, output, n):\n    \"\"\"Best: multiple elements per thread for ILP.\"\"\"\n    base = (cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x) * 4\n    for i in range(4):\n        idx = base + i\n        if idx < n:\n            ai = a[idx]\n            bi = b[idx]\n            output[idx] = math.sqrt(ai * ai + bi * bi)\n\n# --- Setup ---\nn = 8_000_000\nTPB = 256\nBPG = math.ceil(n / TPB)\nBPG_v3 = math.ceil(n / (TPB * 4))  # 4 elements per thread\n\na = cuda.to_device(np.random.randn(n).astype(np.float32))\nb = cuda.to_device(np.random.randn(n).astype(np.float32))\nout = cuda.device_array(n, dtype=np.float32)\n\nprint(\"=== GPU Kernel Optimization: Step by Step ===\")\nprint(f\"Problem: output[i] = sqrt(a[i]^2 + b[i]^2), N = {n:,}\")\nprint(f\"FLOPS/elem: 4 (mul, mul, add, sqrt), Bytes/elem: 12 (2 reads + 1 write)\")\nprint(f\"Arithmetic intensity: {4/12:.2f} FLOPS/byte (memory-bound!)\\n\")\nprint(f\"{'Version':<24} {'Time':>12} | {'BW':>9} | {'Compute':>11} | {'AI'}\")\nprint(\"-\" * 75)\n\nresults = []\nr1 = benchmark(magnitude_v1, BPG, TPB, (a, b, out, n))\nt1 = analyze(\"V1: Naive (x**2)\", r1['mean'], n, 4, 12)\nresults.append(('V1', t1))\n\n# Verify correctness\nout_host = out.copy_to_host()\na_host = a.copy_to_host()\nb_host = b.copy_to_host()\nexpected = np.sqrt(a_host**2 + b_host**2)\nassert np.allclose(out_host, expected, atol=1e-5), \"V1 incorrect!\"\n\nr2 = benchmark(magnitude_v2, BPG, TPB, (a, b, out, n))\nt2 = analyze(\"V2: x*x instead\", r2['mean'], n, 4, 12)\nresults.append(('V2', t2))\n\nr3 = benchmark(magnitude_v3, BPG_v3, TPB, (a, b, out, n))\nt3 = analyze(\"V3: 4 elems/thread\", r3['mean'], n, 4, 12)\nresults.append(('V3', t3))\n\n# Verify V3\nout_host = out.copy_to_host()\nassert np.allclose(out_host, expected, atol=1e-5), \"V3 incorrect!\"\n\n# NumPy reference\ncuda.synchronize()\nstart = time.perf_counter()\nnp_result = np.sqrt(a_host**2 + b_host**2)\nt_np = (time.perf_counter() - start) * 1000\nprint(f\"{'NumPy (CPU)':<24} {t_np:>8.4f} ms |\")\n\nprint(f\"\\n=== Improvement Summary ===\")\nfor name, t in results:\n    print(f\"{name}: {t:.4f} ms ({t1/t:.2f}x vs V1, {t_np/t:.1f}x vs NumPy)\")\n\n# --- Demonstrate the importance of warmup ---\nprint(\"\\n=== Why Warmup Matters ===\")\n\n@cuda.jit\ndef fresh_kernel(a, out, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        out[idx] = a[idx] * 2.0\n\n# First call includes JIT compilation\ncuda.synchronize()\nstart = time.perf_counter()\nfresh_kernel[BPG, TPB](a, out, n)\ncuda.synchronize()\nt_first = (time.perf_counter() - start) * 1000\n\n# Second call is warmed up\ncuda.synchronize()\nstart = time.perf_counter()\nfresh_kernel[BPG, TPB](a, out, n)\ncuda.synchronize()\nt_second = (time.perf_counter() - start) * 1000\n\nprint(f\"First call (includes JIT):  {t_first:.2f} ms\")\nprint(f\"Second call (warmed up):    {t_second:.4f} ms\")\nprint(f\"Ratio: {t_first/t_second:.0f}x -- THIS is why warmup is essential!\")",
      "references": [
        {
          "title": "NVIDIA Nsight Compute Profiling Guide",
          "url": "https://docs.nvidia.com/nsight-compute/ProfilingGuide/index.html"
        },
        {
          "title": "CUDA C Best Practices: Performance Metrics",
          "url": "https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html#performance-metrics"
        },
        {
          "title": "PMPP Chapter 6: Performance Considerations",
          "url": "https://www.elsevier.com/books/programming-massively-parallel-processors/hwu/978-0-323-91231-0"
        },
        {
          "title": "Numba CUDA Performance Tips",
          "url": "https://numba.readthedocs.io/en/stable/cuda/performance.html"
        }
      ]
    }
  ],
  "quiz": [
    {
      "question": "What is memory coalescing?",
      "options": [
        "Combining multiple GPU cores into a single processing unit",
        "The hardware combining memory requests from threads in a warp into fewer transactions",
        "Merging two separate arrays into one for faster access",
        "Caching frequently accessed memory locations in registers"
      ],
      "correct": 1,
      "explanation": "Memory coalescing is when the GPU hardware combines memory access requests from multiple threads in a warp into a minimal number of memory transactions. When consecutive threads access consecutive memory addresses, a single 128-byte transaction can serve all 32 threads, instead of requiring up to 32 separate transactions."
    },
    {
      "question": "A 2D matrix is stored in row-major order. Which access pattern is coalesced?",
      "options": [
        "Consecutive threads reading consecutive rows (same column)",
        "Consecutive threads reading consecutive columns (same row)",
        "Each thread reading a random element",
        "All threads reading the same element"
      ],
      "correct": 1,
      "explanation": "In row-major storage, elements within the same row are consecutive in memory. When consecutive threads read consecutive columns (along a row), they access consecutive memory addresses, producing coalesced access. Reading consecutive rows accesses addresses that are an entire row apart -- severely uncoalesced."
    },
    {
      "question": "A vector addition kernel (c[i] = a[i] + b[i]) has an arithmetic intensity of approximately 0.08 FLOPS/byte. On a T4 GPU (320 GB/s, 8.1 TFLOPS), this kernel is:",
      "options": [
        "Compute-bound: limited by the GPU's FLOPS",
        "Memory-bound: limited by memory bandwidth",
        "Balanced: equally limited by compute and memory",
        "Latency-bound: limited by kernel launch overhead"
      ],
      "correct": 1,
      "explanation": "The T4's ridge point is 8100/320 = 25.3 FLOPS/byte. At 0.08 FLOPS/byte, vector addition is far below the ridge point, making it severely memory-bound. Maximum achievable compute is 0.08 * 320 = 25.6 GFLOPS, which is only 0.3% of the T4's peak. No amount of compute optimization will help -- only memory bandwidth optimization matters."
    },
    {
      "question": "What is the primary benefit of kernel fusion (combining two separate kernels into one)?",
      "options": [
        "It reduces register usage per thread",
        "It eliminates intermediate global memory writes and reads between kernels",
        "It allows using more shared memory",
        "It increases warp-level parallelism"
      ],
      "correct": 1,
      "explanation": "When two kernels run separately, the first writes intermediate results to global memory and the second reads them back. Fusing them into one kernel keeps intermediate values in registers, eliminating the expensive global memory round-trip. For memory-bound kernels, this can provide a 1.5-2x speedup."
    },
    {
      "question": "Why is warmup essential when benchmarking CUDA kernels in numba?",
      "options": [
        "The GPU needs time to reach operating temperature",
        "CUDA contexts require 10 seconds to initialize",
        "The first kernel launch includes JIT compilation overhead that can be 100-1000x the actual kernel time",
        "Memory needs to be pre-fetched from disk to GPU"
      ],
      "correct": 2,
      "explanation": "Numba uses Just-In-Time (JIT) compilation for CUDA kernels. The first call to a kernel triggers compilation from Python to PTX to GPU machine code, which can take 50-500ms. The actual kernel might run in 0.1ms. Without warmup, you measure compilation time, not kernel performance. Always run 5-10 warmup iterations before timing."
    },
    {
      "question": "You measure your kernel achieving 45 GB/s on a T4 GPU (320 GB/s peak bandwidth). You confirmed the kernel is memory-bound. What is the most likely cause?",
      "options": [
        "Low occupancy (not enough warps)",
        "Warp divergence in branch statements",
        "Uncoalesced memory access patterns",
        "Too many kernel launches"
      ],
      "correct": 2,
      "explanation": "At only 15% of peak bandwidth, the most likely cause is uncoalesced memory access. Uncoalesced patterns waste bandwidth by transferring much more data than needed (each warp might trigger 32 transactions instead of 1). Low occupancy and divergence would not typically reduce bandwidth utilization this severely."
    },
    {
      "question": "Why is Structure of Arrays (SoA) preferred over Array of Structures (AoS) for GPU programming?",
      "options": [
        "SoA uses less total memory",
        "SoA allows more threads per block",
        "SoA enables coalesced access when all threads read the same field, since field values are contiguous in memory",
        "SoA is required by the CUDA programming model"
      ],
      "correct": 2,
      "explanation": "In SoA, all x-values are contiguous, all y-values are contiguous, etc. When threads in a warp all read the x-field, they access consecutive addresses (coalesced). In AoS, x-values are interleaved with y and z values, so reading all x-values requires strided access (uncoalesced). SoA doesn't save memory -- it improves access patterns."
    },
    {
      "question": "Your kernel achieves 80% of peak memory bandwidth. Which optimization should you try NEXT?",
      "options": [
        "Improve coalescing -- there is still 20% bandwidth to recover",
        "Increase arithmetic intensity through tiling or algorithmic changes, since memory access is already efficient",
        "Increase occupancy to 100%",
        "Reduce register usage"
      ],
      "correct": 1,
      "explanation": "At 80% of peak bandwidth, your memory access patterns are already very efficient. Further coalescing improvements will yield diminishing returns. The right move is to increase arithmetic intensity -- use tiling/shared memory to reuse loaded data, or fuse operations to do more compute per byte loaded. This shifts the kernel toward compute-bound territory where additional compute is effectively 'free'."
    }
  ],
  "exercise": {
    "title": "Profile and Optimize a Poorly-Written Kernel",
    "description": "You are given a deliberately poorly-written kernel that computes `output[i] = sqrt(a[i]^2 + b[i]^2) + log(abs(a[i]) + 1)` for large arrays. Your task is to profile it, identify the bottlenecks, and apply optimizations step by step.\n\n**Requirements:**\n1. Start with the provided naive kernel and measure its performance\n2. Calculate achieved bandwidth and GFLOPS to determine if memory-bound or compute-bound\n3. Apply at least 3 optimizations, measuring after each one:\n   - Fix the uncoalesced access pattern (the naive version reads from a transposed-like layout)\n   - Replace expensive operations (pow, redundant loads)\n   - Try processing multiple elements per thread\n4. Report a summary table showing time, bandwidth, and speedup for each version\n5. Verify correctness by comparing all versions against numpy\n\n**Hints:**\n- The naive kernel loads data with stride -- fixing this alone may give 3-10x speedup\n- `x ** 2` compiles to a pow() call; `x * x` is a simple multiply (much faster)\n- Loading `a[idx]` twice generates two separate memory transactions -- cache it in a local variable\n- Process 2 or 4 elements per thread for instruction-level parallelism\n- Use `cuda.synchronize()` before and after timing",
    "colabUrl": "https://colab.research.google.com/",
    "starterCode": "!pip install numba\n\nfrom numba import cuda\nimport numpy as np\nimport math\nimport time\n\n# --- The deliberately BAD kernel ---\n@cuda.jit\ndef kernel_naive(a, b, output, n, stride):\n    \"\"\"Poorly written kernel with multiple performance issues.\"\"\"\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        # Issue 1: Strided access (uncoalesced)\n        real_idx = (idx * stride) % n\n        # Issue 2: Using ** (compiled to slow pow())\n        val_a_sq = a[real_idx] ** 2\n        val_b_sq = b[real_idx] ** 2\n        # Issue 3: Redundant global memory load\n        log_val = math.log(abs(a[real_idx]) + 1.0)\n        output[real_idx] = math.sqrt(val_a_sq + val_b_sq) + log_val\n\n# TODO: Implement optimized versions\n# @cuda.jit\n# def kernel_v2(a, b, output, n):  # Fix coalescing\n\n# @cuda.jit\n# def kernel_v3(a, b, output, n):  # Fix operations\n\n# @cuda.jit\n# def kernel_v4(a, b, output, n):  # Multi-element per thread\n\ndef benchmark(kernel, grid, block, args, warmup=10, iters=50):\n    \"\"\"Time a kernel, return mean time in ms.\"\"\"\n    # TODO: implement with warmup and synchronization\n    pass\n\n# Setup\nn = 4_000_000\nTPB = 256\nBPG = math.ceil(n / TPB)\n\n# TODO: Create data, benchmark each version, verify correctness, print results table",
    "solution": "!pip install numba\n\nfrom numba import cuda\nimport numpy as np\nimport math\nimport time\n\n# --- Version 1: Deliberately BAD kernel ---\n@cuda.jit\ndef kernel_naive(a, b, output, n, stride):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        real_idx = (idx * stride) % n  # Strided = uncoalesced\n        val_a_sq = a[real_idx] ** 2     # Slow pow()\n        val_b_sq = b[real_idx] ** 2     # Slow pow()\n        log_val = math.log(abs(a[real_idx]) + 1.0)  # Redundant load\n        output[real_idx] = math.sqrt(val_a_sq + val_b_sq) + log_val\n\n# --- Version 2: Fix coalescing (sequential access) ---\n@cuda.jit\ndef kernel_v2(a, b, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        val_a_sq = a[idx] ** 2\n        val_b_sq = b[idx] ** 2\n        log_val = math.log(abs(a[idx]) + 1.0)\n        output[idx] = math.sqrt(val_a_sq + val_b_sq) + log_val\n\n# --- Version 3: Fix operations (multiply, cache loads) ---\n@cuda.jit\ndef kernel_v3(a, b, output, n):\n    idx = cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x\n    if idx < n:\n        ai = a[idx]  # Load once, reuse\n        bi = b[idx]\n        output[idx] = math.sqrt(ai * ai + bi * bi) + math.log(abs(ai) + 1.0)\n\n# --- Version 4: Multi-element per thread ---\n@cuda.jit\ndef kernel_v4(a, b, output, n):\n    base = (cuda.threadIdx.x + cuda.blockIdx.x * cuda.blockDim.x) * 4\n    for i in range(4):\n        idx = base + i\n        if idx < n:\n            ai = a[idx]\n            bi = b[idx]\n            output[idx] = math.sqrt(ai * ai + bi * bi) + math.log(abs(ai) + 1.0)\n\ndef benchmark(kernel, grid, block, args, warmup=10, iters=50):\n    for _ in range(warmup):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    start = time.perf_counter()\n    for _ in range(iters):\n        kernel[grid, block](*args)\n    cuda.synchronize()\n    return (time.perf_counter() - start) / iters * 1000\n\n# Setup\nn = 4_000_000\nTPB = 256\nBPG = math.ceil(n / TPB)\nBPG_v4 = math.ceil(n / (TPB * 4))\n\na_host = np.random.randn(n).astype(np.float32)\nb_host = np.random.randn(n).astype(np.float32)\nexpected = np.sqrt(a_host**2 + b_host**2) + np.log(np.abs(a_host) + 1.0)\n\nd_a = cuda.to_device(a_host)\nd_b = cuda.to_device(b_host)\nd_out = cuda.device_array(n, dtype=np.float32)\n\nbytes_per_elem = 12  # 2 reads + 1 write, float32\nflops_per_elem = 6   # mul, mul, add, sqrt, abs+add, log\n\nprint(f\"Problem: output[i] = sqrt(a[i]^2 + b[i]^2) + log(|a[i]| + 1)\")\nprint(f\"N = {n:,}, AI = {flops_per_elem/bytes_per_elem:.2f} FLOPS/byte\\n\")\nprint(f\"{'Version':<30} {'Time (ms)':<12} {'BW (GB/s)':<12} {'GFLOPS':<10} {'Speedup'}\")\nprint(\"-\" * 76)\n\n# V1: Naive (strided)\nt1 = benchmark(kernel_naive, BPG, TPB, (d_a, d_b, d_out, n, 7))\nbw1 = n * bytes_per_elem / (t1/1000) / 1e9\ngf1 = n * flops_per_elem / (t1/1000) / 1e9\nprint(f\"{'V1: Naive (strided+pow)':<30} {t1:<12.4f} {bw1:<12.1f} {gf1:<10.1f} {'1.00x'}\")\n\nout1 = d_out.copy_to_host()\n\n# V2: Fixed coalescing\nt2 = benchmark(kernel_v2, BPG, TPB, (d_a, d_b, d_out, n))\nbw2 = n * bytes_per_elem / (t2/1000) / 1e9\ngf2 = n * flops_per_elem / (t2/1000) / 1e9\nprint(f\"{'V2: Coalesced access':<30} {t2:<12.4f} {bw2:<12.1f} {gf2:<10.1f} {t1/t2:.2f}x\")\nassert np.allclose(d_out.copy_to_host(), expected, atol=1e-4)\n\n# V3: Fixed operations\nt3 = benchmark(kernel_v3, BPG, TPB, (d_a, d_b, d_out, n))\nbw3 = n * bytes_per_elem / (t3/1000) / 1e9\ngf3 = n * flops_per_elem / (t3/1000) / 1e9\nprint(f\"{'V3: x*x + cached loads':<30} {t3:<12.4f} {bw3:<12.1f} {gf3:<10.1f} {t1/t3:.2f}x\")\nassert np.allclose(d_out.copy_to_host(), expected, atol=1e-4)\n\n# V4: Multi-element\nt4 = benchmark(kernel_v4, BPG_v4, TPB, (d_a, d_b, d_out, n))\nbw4 = n * bytes_per_elem / (t4/1000) / 1e9\ngf4 = n * flops_per_elem / (t4/1000) / 1e9\nprint(f\"{'V4: 4 elems/thread':<30} {t4:<12.4f} {bw4:<12.1f} {gf4:<10.1f} {t1/t4:.2f}x\")\nassert np.allclose(d_out.copy_to_host(), expected, atol=1e-4)\n\nprint(f\"\\nTotal speedup (V1 -> V4): {t1/t4:.1f}x\")\nprint(f\"Bandwidth improved: {bw1:.1f} -> {bw4:.1f} GB/s\")"
  }
}
