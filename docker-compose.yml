services:
  llmlearn:
    build: .
    container_name: llmlearn
    restart: unless-stopped
    ports:
      - "3080:3001"
    volumes:
      # Mount host claude config so claude -p works in container
      - /home/wouter/.claude:/root/.claude:ro
    environment:
      - PORT=3001
    networks:
      - npm

networks:
  npm:
    external: true
    name: opt_default
